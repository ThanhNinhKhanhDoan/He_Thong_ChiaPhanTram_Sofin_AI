<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <!-- SEO Meta Tags -->
    <meta name="description" content="Contact SOFIN NETWORK for AI solutions, technology consulting, digital content distribution, and training services. Get in touch with our global team today.">
    <meta name="keywords" content="contact SOFIN NETWORK, AI consulting, technology services contact, digital content distribution, AI training, global office locations, business cooperation">
    <meta name="author" content="SOFIN NETWORK">
    <meta name="robots" content="index, follow">

    <!-- Open Graph / Facebook -->
    <meta property="og:title" content="Contact Us | SOFIN NETWORK Global AI & Technology Services">
    <meta property="og:description" content="Contact SOFIN NETWORK for AI solutions, technology consulting, digital content distribution, and training services. Get in touch with our global team today.">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://sofin.network/public/contact">
    <meta property="og:image" content="https://sofin.network/images/thumnail.jpeg">
    <meta property="og:site_name" content="SOFIN NETWORK">

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="@sofin_network">
    <meta name="twitter:title" content="Contact Us | SOFIN NETWORK Global AI & Technology Services">
    <meta name="twitter:description" content="Contact SOFIN NETWORK for AI solutions, technology consulting, digital content distribution, and training services. Get in touch with our global team today.">
    <meta name="twitter:image" content="https://sofin.network/images/thumnail.jpeg">

    <!-- Favicon & Title -->
    <title>Contact Us | SOFIN NETWORK Global AI & Technology Services</title>

    
    <link rel="icon" type="image/png" href="<?= asset("/style/acom-all/img/favicon/favicon-196x196.png") ?>" sizes="196x196">
    <link rel="icon" type="image/png" href="<?= asset("/style/acom-all/img/favicon/favicon-96x96.png") ?>" sizes="96x96">
    <link rel="icon" type="image/png" href="<?= asset("/style/acom-all/img/favicon/favicon-32x32.png") ?>" sizes="32x32">
    <link rel="icon" type="image/png" href="<?= asset("/style/acom-all/img/favicon/favicon-16x16.png") ?>" sizes="16x16">
    <link rel="icon" type="image/png" href="<?= asset("/style/acom-all/img/favicon/favicon-128.png") ?>" sizes="128x128">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500&family=Cormorant+Garamond:wght@300;400;500;600;700&family=Dancing+Script:wght@400;500;600;700&family=Josefin+Sans:wght@300;400;500;600;700&family=Cinzel:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">

    <style>
        :root {
            /* Dark Mode Color Palette */
            --color-1: #7400b8;
            --color-2: #6930c3;
            --color-3: #5e60ce;
            --color-4: #5390d9;
            --color-5: #4ea8de;
            --color-6: #48bfe3;
            --color-7: #56cfe1;
            --color-8: #64dfdf;
            --color-9: #72efdd;
            --color-10: #80ffdb;
            --text-light: #ffffff;
            --text-dark: #1a1a1a;
            --text-muted: rgba(255, 255, 255, 0.8);
            --dark-bg: #0d0221;
            --dark-card: rgba(255, 255, 255, 0.05);
            --glass-bg: rgba(255, 255, 255, 0.1);
            --glass-border: rgba(255, 255, 255, 0.2);
            --glass-shadow: 0 8px 32px 0 rgba(116, 0, 184, 0.37);
            --gradient-1: linear-gradient(135deg, var(--color-1), var(--color-3));
            --gradient-2: linear-gradient(135deg, var(--color-3), var(--color-6));
            --gradient-3: linear-gradient(135deg, var(--color-6), var(--color-10));
            --gradient-4: linear-gradient(135deg, var(--color-1), var(--color-10));
            --gradient-5: linear-gradient(45deg, var(--color-1), var(--color-3), var(--color-6), var(--color-10));
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            /* Font Variables */
            --heading-font: 'Playfair Display', serif;
            --body-font: 'Cormorant Garamond', serif;
            --accent-font: 'Dancing Script', cursive;
            --nav-font: 'Josefin Sans', sans-serif;
            --logo-font: 'Cinzel', serif;
        }

        /* Enhanced Light Mode Colors - Balanced for readability */
        html.light-mode {
            /* Primary Colors - Still soft but with more contrast */
            --color-1: #6b5b95;
            --color-2: #7d6ca7;
            --color-3: #8d7dba;
            --color-4: #4a90c2;
            --color-5: #5ba3db;
            --color-6: #6bb6e5;
            --color-7: #4fc3f7;
            --color-8: #51bcd4;
            --color-9: #66d9ef;
            --color-10: #5cb85c;
            
            /* Text Colors - Much darker for better readability */
            --text-light: #1a1a1a;
            --text-dark: #000000;
            --text-muted: rgba(60, 60, 60, 0.9);
            
            /* Background Colors - Still soft but with enough contrast */
            --dark-bg: #f8f9fa;
            --dark-card: rgba(255, 255, 255, 0.85);
            
            /* Glass Effect - More transparent but borders more visible */
            --glass-bg: rgba(255, 255, 255, 0.8);
            --glass-border: rgba(107, 91, 149, 0.15);
            --glass-shadow: 0 8px 32px 0 rgba(107, 91, 149, 0.1);
            
            /* Light Mode Gradients - Softer transitions but still vibrant */
            --gradient-1: linear-gradient(135deg, var(--color-1), var(--color-3));
            --gradient-2: linear-gradient(135deg, var(--color-3), var(--color-6));
            --gradient-3: linear-gradient(135deg, var(--color-6), var(--color-10));
            --gradient-4: linear-gradient(135deg, var(--color-1), var(--color-10));
            --gradient-5: linear-gradient(45deg, var(--color-1), var(--color-3), var(--color-6), var(--color-10));
        }

        /* PERFORMANCE OPTIMIZATIONS */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Force GPU acceleration for smooth scrolling */
        .optimized-element,
        .hero-section,
        .contact-form-section,
        .contact-info-card,
        .map-container,
        .navbar,
        .logo-circle,
        .orbiting-dot {
            transform: translateZ(0);
            -webkit-backface-visibility: hidden;
            backface-visibility: hidden;
            will-change: transform;
        }

        /* LOADER CSS */
        .loader-wrapper {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: var(--dark-bg);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.5s ease, visibility 0.5s ease;
        }

        .loader-logo-container {
            position: relative;
            width: 250px;
            height: 250px;
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 30px;
        }

        .loader-logo-circle {
            position: absolute;
            border-radius: 50%;
            border: 2px solid transparent;
            box-sizing: border-box;
            opacity: 0.7;
            pointer-events: none;
            filter: drop-shadow(0 0 10px rgba(116, 0, 184, 0.5));
        }

        .loader-circle-1 {
            width: 80px;
            height: 80px;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            border-image: linear-gradient(45deg, var(--color-1), var(--color-3)) 1;
            animation: rotate 6s linear infinite;
        }

        .loader-circle-2 {
            width: 120px;
            height: 120px;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            border-image: linear-gradient(135deg, var(--color-3), var(--color-6)) 1;
            animation: rotate 10s linear infinite reverse;
        }

        .loader-circle-3 {
            width: 160px;
            height: 160px;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            border-image: linear-gradient(225deg, var(--color-6), var(--color-10)) 1;
            animation: rotate 14s linear infinite;
        }

        .loader-circle-4 {
            width: 200px;
            height: 200px;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            border-image: linear-gradient(315deg, var(--color-1), var(--color-10)) 1;
            animation: rotate 18s linear infinite reverse;
        }

        .loader-logo-text {
            position: relative;
            z-index: 15;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            animation: pulseLogo 2s ease-in-out infinite alternate;
            filter: drop-shadow(0 0 20px rgba(116, 0, 184, 0.6));
        }

        .loader-logo-main {
            font-family: var(--logo-font);
            font-size: 3.5rem;
            font-weight: 900;
            letter-spacing: 4px;
            background: linear-gradient(90deg, var(--color-1), var(--color-3), var(--color-6), var(--color-10));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            background-size: 300% 100%;
            animation: gradientShift 8s ease infinite;
            line-height: 1;
        }

        .loader-logo-tagline {
            font-size: 1.1rem;
            font-weight: 500;
            letter-spacing: 4px;
            color: var(--color-7);
            margin-top: 5px;
            opacity: 0.9;
            text-transform: uppercase;
            font-family: var(--nav-font);
        }

        .loader-dots-container {
            position: absolute;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
        }

        .loader-logo-dot {
            position: absolute;
            width: 6px;
            height: 6px;
            background-color: var(--color-7);
            border-radius: 50%;
            box-shadow: 0 0 8px var(--color-7);
            animation: sparkle 2s infinite alternate;
            opacity: 0.8;
            pointer-events: none;
        }

        .loader-orbiting-dot {
            position: absolute;
            width: 8px;
            height: 8px;
            background: var(--color-10);
            border-radius: 50%;
            box-shadow: 0 0 15px var(--color-10);
            transform-origin: center;
            opacity: 0.8;
            pointer-events: none;
            z-index: 3;
        }

        .loader {
            position: relative;
            width: 100px;
            height: 8px;
            margin-top: 20px;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 4px;
            overflow: hidden;
        }

        .loader::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            height: 100%;
            width: 0;
            background: linear-gradient(90deg, var(--color-1), var(--color-6), var(--color-10));
            animation: loading 3s infinite;
            border-radius: 4px;
        }

        @keyframes loading {
            0% { width: 0; }
            50% { width: 100%; }
            100% { width: 100%; }
        }

        @keyframes rotate {
            from { transform: translate(-50%, -50%) rotate(0deg); }
            to { transform: translate(-50%, -50%) rotate(360deg); }
        }

        @keyframes pulseLogo {
            0% {
                transform: scale(1);
                filter: brightness(1);
            }
            100% {
                transform: scale(1.05);
                filter: brightness(1.2);
            }
        }

        @keyframes sparkle {
            0% {
                transform: scale(1);
                box-shadow: 0 0 8px var(--color-7);
            }
            100% {
                transform: scale(1.5);
                box-shadow: 0 0 20px var(--color-9);
            }
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Main CSS */
        body {
            font-family: var(--body-font);
            background-color: var(--dark-bg);
            color: var(--text-light);
            overflow-x: hidden;
            font-weight: 400;
            line-height: 1.6;
            font-size: 1.1rem;
            scroll-behavior: smooth;
            padding-top: 0;
        }

        /* Light Mode Body Background - Softer but still good contrast */
        html.light-mode body {
            background-image: 
                radial-gradient(circle at 20% 30%, rgba(107, 91, 149, 0.04) 0%, transparent 50%),
                radial-gradient(circle at 80% 40%, rgba(125, 108, 167, 0.04) 0%, transparent 50%),
                radial-gradient(circle at 40% 70%, rgba(141, 125, 186, 0.04) 0%, transparent 50%),
                radial-gradient(circle at 70% 90%, rgba(107, 182, 229, 0.04) 0%, transparent 50%);
            background-size: 150% 150%;
            background-attachment: fixed;
            position: relative;
            min-height: 100vh;
            background-color: #f8f9fa;
        }

        /* Optimized dark mode background */
        body {
            background-image: 
                radial-gradient(circle at 20% 30%, rgba(116, 0, 184, 0.3) 0%, transparent 40%),
                radial-gradient(circle at 80% 40%, rgba(94, 96, 206, 0.3) 0%, transparent 40%),
                radial-gradient(circle at 40% 70%, rgba(72, 191, 227, 0.3) 0%, transparent 40%),
                radial-gradient(circle at 70% 90%, rgba(128, 255, 219, 0.3) 0%, transparent 40%);
            background-size: 100% 100%;
            background-attachment: fixed;
            position: relative;
            min-height: 100vh;
        }

        /* Light Mode animated background - Even more subtle */
        html.light-mode body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                radial-gradient(1px 1px at 20% 30%, rgba(139, 125, 168, 0.15), transparent),
                radial-gradient(1px 1px at 40% 70%, rgba(156, 195, 229, 0.15), transparent),
                radial-gradient(1px 1px at 90% 40%, rgba(156, 195, 229, 0.15), transparent);
            background-size: 400px 400px, 350px 350px, 300px 300px;
            animation: moveBackground 40s infinite linear;
            z-index: -2;
            transform: translateZ(0);
            opacity: 0.15;
        }

        /* Optimized animated background for dark mode*/
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                radial-gradient(2px 2px at 20% 30%, rgba(116, 0, 184, 0.8), transparent),
                radial-gradient(2px 2px at 40% 70%, rgba(72, 191, 227, 0.8), transparent),
                radial-gradient(1px 1px at 90% 40%, rgba(128, 255, 219, 0.8), transparent);
            background-size: 200px 200px, 150px 150px, 100px 100px;
            animation: moveBackground 20s infinite linear;
            z-index: -2;
            transform: translateZ(0);
        }

        @keyframes moveBackground {
            0% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(30px, -30px) rotate(120deg); }
            66% { transform: translate(-20px, 20px) rotate(240deg); }
            100% { transform: translate(0, 0) rotate(360deg); }
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: var(--heading-font);
            font-weight: 700;
            letter-spacing: 0.5px;
            line-height: 1.3;
            margin-bottom: 0;
        }

        /* OPTIMIZED LOGO SECTION */
        .logo-container {
            position: relative;
            width: 160px;
            height: 60px;
            display: flex;
            justify-content: center;
            align-items: center;
            transition: var(--transition);
        }

        .navbar-brand {
            display: flex;
            align-items: center;
            position: relative;
            z-index: 10;
            font-family: var(--heading-font);
            font-size: 2rem;
            font-weight: 700;
            color: var(--text-light);
            letter-spacing: 2px;
            transition: var(--transition);
            text-decoration: none;
        }

        .navbar-brand:hover {
            color: var(--text-light);
        }

        .logo-text {
            position: relative;
            z-index: 15;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            filter: drop-shadow(0 0 20px rgba(116, 0, 184, 0.6));
        }

        .logo-main {
            font-family: var(--logo-font);
            font-size: 1.8rem;
            font-weight: 900;
            letter-spacing: 2px;
            background: linear-gradient(90deg, var(--color-1), var(--color-3), var(--color-6), var(--color-10));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            background-size: 300% 100%;
            animation: gradientShift 8s ease infinite;
            line-height: 1;
        }

        .logo-tagline {
            font-size: 0.65rem;
            font-weight: 500;
            letter-spacing: 3px;
            color: var(--color-7);
            margin-top: 2px;
            opacity: 0.9;
            text-transform: uppercase;
            font-family: var(--nav-font);
        }

        .logo-circle {
            position: absolute;
            border-radius: 50%;
            border: 1px solid transparent;
            box-sizing: border-box;
            opacity: 0.7;
            pointer-events: none;
            filter: drop-shadow(0 0 10px rgba(116, 0, 184, 0.5));
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .circle-1 {
            width: 25px;
            height: 25px;
            border-image: linear-gradient(45deg, var(--color-1), var(--color-3)) 1;
            animation: rotate 8s linear infinite;
        }

        .circle-2 {
            width: 40px;
            height: 40px;
            border-image: linear-gradient(135deg, var(--color-3), var(--color-6)) 1;
            animation: rotate 12s linear infinite reverse;
        }

        .circle-3 {
            width: 55px;
            height: 55px;
            border-image: linear-gradient(225deg, var(--color-6), var(--color-10)) 1;
            animation: rotate 16s linear infinite;
        }

        /* Light Mode Logo Circle Adjustments */
        html.light-mode .logo-circle {
            filter: drop-shadow(0 0 5px rgba(139, 125, 168, 0.2));
        }

        .logo-dot {
            position: absolute;
            width: 4px;
            height: 4px;
            background-color: var(--color-7);
            border-radius: 50%;
            box-shadow: 0 0 10px var(--color-7);
            opacity: 0.8;
            pointer-events: none;
            animation: sparkle 2s ease-in-out infinite alternate;
        }

        .orbiting-dot {
            position: absolute;
            width: 5px;
            height: 5px;
            background: var(--color-10);
            border-radius: 50%;
            box-shadow: 0 0 15px var(--color-10);
            transform-origin: center;
            opacity: 0.8;
            pointer-events: none;
            z-index: 3;
        }

        /* OPTIMIZED NAVBAR */
        .navbar {
            padding: 20px 0;
            transition: var(--transition);
            position: fixed;
            width: 100%;
            z-index: 1030;
            top: 0;
            left: 0;
            right: 0;
            background: transparent;
        }

        .navbar.scrolled {
            background: var(--glass-bg);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            padding: 15px 0;
            box-shadow: var(--glass-shadow);
            border-bottom: 1px solid var(--glass-border);
        }

        /* Light Mode Navbar Improvements */
        html.light-mode .navbar.scrolled {
            background: rgba(248, 249, 250, 0.95);
            backdrop-filter: blur(20px);
            box-shadow: 0 2px 20px rgba(139, 125, 168, 0.05);
            border-bottom: 1px solid rgba(139, 125, 168, 0.08);
        }

        .nav-link {
            font-family: var(--nav-font);
            font-weight: 500;
            margin: 0 15px;
            padding: 10px 0;
            position: relative;
            color: var(--text-light) !important;
            font-size: 1rem;
            letter-spacing: 0.5px;
            transition: var(--transition);
            text-decoration: none;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: 5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--gradient-3);
            transition: var(--transition);
        }

        .nav-link:hover::after,
        .nav-link.active::after {
            width: 100%;
        }

        .nav-link:hover,
        .nav-link.active {
            color: var(--color-8) !important;
            text-shadow: 0 0 10px rgba(84, 144, 217, 0.6);
        }

        /* Light Mode Nav Link Text Shadow */
        html.light-mode .nav-link:hover,
        html.light-mode .nav-link.active {
            text-shadow: 0 0 8px rgba(139, 125, 168, 0.2);
        }

        /* Navbar toggler */
        .navbar-toggler {
            border: none;
            outline: none !important;
            padding: 0;
            cursor: pointer;
            background-color: transparent;
        }

        .navbar-toggler-icon {
            display: inline-block;
            width: 30px;
            height: 2px;
            background-color: var(--text-light);
            position: relative;
            transition: var(--transition);
        }

        .navbar-toggler-icon::before,
        .navbar-toggler-icon::after {
            content: '';
            position: absolute;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: var(--text-light);
            transition: var(--transition);
        }

        .navbar-toggler-icon::before {
            top: -8px;
        }

        .navbar-toggler-icon::after {
            top: 8px;
        }

        .navbar-toggler[aria-expanded="true"] .navbar-toggler-icon {
            background-color: transparent;
        }

        .navbar-toggler[aria-expanded="true"] .navbar-toggler-icon::before {
            transform: translateY(8px) rotate(45deg);
        }

        .navbar-toggler[aria-expanded="true"] .navbar-toggler-icon::after {
            transform: translateY(-8px) rotate(-45deg);
        }

        /* HERO SECTION */
        .hero-section {
            height: 70vh;
            min-height: 600px;
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            padding-top: 80px;
        }

        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient-1);
            opacity: 0.1;
            z-index: 1;
        }

        /* Light Mode Hero Overlay */
        html.light-mode .hero-overlay {
            background: var(--gradient-1);
            opacity: 0.02;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                radial-gradient(circle at 20% 20%, rgba(116, 0, 184, 0.4) 1px, transparent 1px),
                radial-gradient(circle at 80% 80%, rgba(128, 255, 219, 0.4) 1px, transparent 1px),
                radial-gradient(circle at 60% 30%, rgba(72, 191, 227, 0.4) 1px, transparent 1px);
            background-size: 50px 50px, 80px 80px, 40px 40px;
            animation: Float 10s ease-in-out infinite;
            z-index: 0;
        }

        /* Light Mode Hero Section Pattern */
        html.light-mode .hero-section::before {
            background-image: 
                radial-gradient(circle at 20% 20%, rgba(139, 125, 168, 0.08) 1px, transparent 1px),
                radial-gradient(circle at 80% 80%, rgba(156, 195, 229, 0.08) 1px, transparent 1px),
                radial-gradient(circle at 60% 30%, rgba(156, 195, 229, 0.08) 1px, transparent 1px);
            opacity: 0.3;
            background-size: 80px 80px, 100px 100px, 60px 60px;
        }

        @keyframes Float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }

        .hero-content {
            position: relative;
            z-index: 2;
            text-align: center;
            max-width: 900px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .hero-subtitle {
            font-family: var(--accent-font);
            font-size: 2.8rem;
            margin-bottom: 1rem;
            color: var(--color-7);
            font-weight: 500;
            animation: fadeInUp 1s ease;
            text-shadow: 0 0 20px rgba(86, 207, 225, 0.5);
        }

        /* Light Mode Hero Subtitle */
        html.light-mode .hero-subtitle {
            text-shadow: 0 0 10px rgba(156, 195, 229, 0.15);
        }

        .hero-title {
            font-size: 3.5rem;
            font-weight: 700;
            line-height: 1.1;
            margin-bottom: 1.5rem;
            animation: fadeInUp 1s ease 0.2s both;
            text-shadow: 0 0 30px rgba(116, 0, 184, 0.5);
        }

        /* Light Mode Hero Title */
        html.light-mode .hero-title {
            text-shadow: 0 0 20px rgba(107, 91, 149, 0.15);
        }

        .hero-title .main-text {
            background: linear-gradient(90deg, var(--color-1), var(--color-3), var(--color-6), var(--color-8), var(--color-10), var(--color-8), var(--color-6), var(--color-3), var(--color-1));
            background-size: 300% 100%;
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            animation: flowingGradient 3s ease-in-out infinite;
            font-size: 4.8rem;
            font-weight: 700;
            line-height: 1.1;
            filter: drop-shadow(0 0 20px rgba(116, 0, 184, 0.5));
        }

        /* Light Mode Main Text Filter */
        html.light-mode .hero-title .main-text {
            filter: drop-shadow(0 0 10px rgba(139, 125, 168, 0.08));
        }

        @keyframes flowingGradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .hero-description {
            font-size: 1.6rem;
            margin-bottom: 2rem;
            color: var(--text-muted);
            animation: fadeInUp 1s ease 0.4s both;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.3);
        }

        /* Light Mode Hero Description */
        html.light-mode .hero-description {
            text-shadow: 0 0 2px rgba(139, 125, 168, 0.05);
        }

        /* FLOATING BUTTONS */
        .theme-switch,
        .language-switch,
        .scroll-to-top {
            position: fixed;
            z-index: 1040;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            transition: var(--transition);
            overflow: hidden;
            border: none;
        }

        .theme-switch {
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            background: var(--gradient-1);
            color: var(--text-light);
            box-shadow: 0 5px 15px rgba(116, 0, 184, 0.4);
        }

        .language-switch {
            bottom: 100px;
            right: 30px;
            width: 60px;
            height: 60px;
            background: var(--gradient-2);
            color: var(--text-light);
            box-shadow: 0 5px 15px rgba(105, 48, 195, 0.4);
        }

        .scroll-to-top {
            bottom: 30px;
            left: 30px;
            width: 50px;
            height: 50px;
            background: var(--gradient-2);
            color: white;
            display: none;
            opacity: 0;
            transform: translateY(20px);
            box-shadow: 0 4px 15px rgba(105, 48, 195, 0.3);
        }

        .scroll-to-top.show {
            display: flex;
            opacity: 1;
            transform: translateY(0);
        }

        .theme-switch:hover,
        .language-switch:hover,
        .scroll-to-top:hover {
            transform: translateY(-5px) scale(1.1);
        }

        .theme-switch:hover {
            box-shadow: 0 10px 20px rgba(116, 0, 184, 0.6);
        }

        .language-switch:hover {
            box-shadow: 0 10px 20px rgba(105, 48, 195, 0.6);
        }

        .scroll-to-top:hover {
            box-shadow: 0 8px 25px rgba(105, 48, 195, 0.5);
        }

        /* Light Mode Floating Button Improvements */
        html.light-mode .theme-switch,
        html.light-mode .language-switch {
            color: #ffffff;
        }

        /* CONTACT SECTIONS */
        .contact-section {
            padding: 80px 0;
            position: relative;
        }

        .section-title {
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 1.5rem;
            background: var(--gradient-1);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            background-size: 200% 200%;
            animation: gradientShift 4s ease infinite;
            position: relative;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -15px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 4px;
            background: var(--gradient-3);
            border-radius: 2px;
        }

        .section-subtitle {
            text-align: center;
            font-size: 1.2rem;
            color: var(--text-muted);
            margin-bottom: 3rem;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }

        /* CONTACT INFO CARDS */
        .contact-info-cards {
            margin-bottom: 60px;
        }

        .contact-info-card {
            background: var(--glass-bg);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            border-radius: 25px;
            padding: 40px;
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            transition: var(--transition);
            box-shadow: var(--glass-shadow);
            margin-bottom: 30px;
        }

        /* Light Mode Contact Info Card */
        html.light-mode .contact-info-card {
            background: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(107, 91, 149, 0.12);
            box-shadow: 0 8px 32px rgba(107, 91, 149, 0.1);
        }

        .contact-info-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(116, 0, 184, 0.4);
        }

        /* Light Mode Contact Info Card Hover */
        html.light-mode .contact-info-card:hover {
            box-shadow: 0 20px 40px rgba(139, 125, 168, 0.12);
        }

        .contact-info-icon {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            background: var(--gradient-3);
            color: var(--text-dark);
            margin-bottom: 25px;
            box-shadow: 0 5px 15px rgba(72, 191, 227, 0.3);
        }

        /* Light Mode Contact Icon */
        html.light-mode .contact-info-icon {
            color: #ffffff;
        }

        .contact-info-content h5 {
            font-size: 1.3rem;
            margin-bottom: 10px;
            color: var(--text-light);
        }

        .contact-info-content p {
            color: var(--text-muted);
            margin-bottom: 15px;
            font-size: 1rem;
            line-height: 1.6;
        }

        .contact-info-content a {
            color: var(--color-7);
            text-decoration: none;
            transition: var(--transition);
        }

        .contact-info-content a:hover {
            color: var(--color-9);
            text-shadow: 0 0 10px rgba(72, 191, 227, 0.5);
        }

        /* Light Mode Contact Link Hover */
        html.light-mode .contact-info-content a:hover {
            text-shadow: 0 0 8px rgba(79, 175, 219, 0.3);
        }

        .contact-social {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }

        .contact-social a {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--text-light);
            font-size: 1.2rem;
            transition: var(--transition);
            border: 1px solid var(--glass-border);
        }

        .contact-social a:hover {
            background: var(--gradient-3);
            color: var(--text-dark);
            transform: translateY(-5px);
        }

        /* Light Mode Contact Social Hover */
        html.light-mode .contact-social a:hover {
            color: #ffffff;
        }

        /* CONTACT FORM */
        .contact-form-section {
            background: var(--glass-bg);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            border-radius: 30px;
            padding: 50px;
            box-shadow: var(--glass-shadow);
            margin-bottom: 60px;
        }

        /* Light Mode Contact Form */
        html.light-mode .contact-form-section {
            background: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(107, 91, 149, 0.12);
            box-shadow: 0 8px 32px rgba(107, 91, 149, 0.1);
        }

        .contact-form-header {
            margin-bottom: 30px;
            text-align: center;
        }

        .contact-form-title {
            font-size: 2.2rem;
            font-weight: 700;
            margin-bottom: 15px;
            background: var(--gradient-1);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .contact-form-subtitle {
            color: var(--text-muted);
            font-size: 1.1rem;
            line-height: 1.7;
        }

        .form-group {
            margin-bottom: 25px;
            position: relative;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: var(--text-light);
            font-family: var(--nav-font);
            letter-spacing: 0.5px;
        }

        .form-control {
            width: 100%;
            padding: 15px 20px;
            border: 2px solid var(--glass-border);
            border-radius: 20px;
            background: var(--glass-bg);
            color: var(--text-light);
            font-size: 1rem;
            font-family: var(--body-font);
            transition: var(--transition);
            outline: none;
            resize: vertical;
            min-height: 50px;
        }

        /* Light Mode Form Control */
        html.light-mode .form-control {
            background: rgba(255, 255, 255, 0.8);
            border: 2px solid rgba(107, 91, 149, 0.15);
            color: var(--text-dark);
        }

        .form-control::placeholder {
            color: var(--text-muted);
            opacity: 0.7;
        }

        .form-control:focus {
            border-color: var(--color-6);
            box-shadow: 0 0 20px rgba(72, 191, 227, 0.3);
            background: rgba(255, 255, 255, 0.1);
        }

        /* Light Mode Form Control Focus */
        html.light-mode .form-control:focus {
            background: rgba(255, 255, 255, 0.95);
            border-color: var(--color-6);
            box-shadow: 0 0 15px rgba(79, 175, 219, 0.2);
        }

        textarea.form-control {
            min-height: 120px;
            resize: vertical;
        }

        .btn-primary-custom {
            background: var(--gradient-3);
            border: none;
            color: var(--text-dark);
            padding: 15px 40px;
            border-radius: 30px;
            font-weight: 600;
            font-family: var(--nav-font);
            letter-spacing: 1px;
            transition: var(--transition);
            cursor: pointer;
            box-shadow: 0 5px 15px rgba(72, 191, 227, 0.3);
            display: inline-flex;
            align-items: center;
            gap: 10px;
            font-size: 1rem;
        }

        /* Light Mode Button */
        html.light-mode .btn-primary-custom {
            color: #ffffff;
        }

        .btn-primary-custom:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(72, 191, 227, 0.5);
        }

        .btn-primary-custom:active {
            transform: translateY(-2px);
        }

        /* MAP SECTION */
        .map-container {
            background: var(--glass-bg);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            border-radius: 30px;
            padding: 30px;
            box-shadow: var(--glass-shadow);
            overflow: hidden;
            position: relative;
        }

        /* Light Mode Map Container */
        html.light-mode .map-container {
            background: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(107, 91, 149, 0.12);
            box-shadow: 0 8px 32px rgba(107, 91, 149, 0.1);
        }

        .map-wrapper {
            border-radius: 20px;
            overflow: hidden;
            height: 400px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        /* Light Mode Map Wrapper */
        html.light-mode .map-wrapper {
            box-shadow: 0 10px 30px rgba(139, 125, 168, 0.1);
        }

        iframe {
            width: 100%;
            height: 100%;
            border: none;
            border-radius: 20px;
        }

        .map-overlay {
            position: absolute;
            top: 30px;
            left: 30px;
            max-width: 320px;
            background: var(--glass-bg);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            box-shadow: var(--glass-shadow);
            border-radius: 15px;
            padding: 25px;
            z-index: 2;
        }

        .map-card-title {
            font-size: 1.4rem;
            font-weight: 700;
            margin-bottom: 15px;
            line-height: 1.3;
            color: #00B3F1;
        }

        .map-card-address {
            color: #000;
            font-size: 1rem;
            line-height: 1.5;
            margin-bottom: 15px;
        }

        /* OFFICE CARDS */
        .contact-offices {
            margin-top: 50px;
        }

        .office-card {
            background: var(--glass-bg);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            box-shadow: var(--glass-shadow);
            border-radius: 20px;
            padding: 30px;
            height: 100%;
            transition: var(--transition);
        }

        /* Light Mode Office Card */
        html.light-mode .office-card {
            background: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(107, 91, 149, 0.12);
            box-shadow: 0 8px 32px rgba(107, 91, 149, 0.1);
        }

        .office-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(116, 0, 184, 0.4);
        }

        /* Light Mode Office Card Hover */
        html.light-mode .office-card:hover {
            box-shadow: 0 15px 30px rgba(139, 125, 168, 0.12);
        }

        .office-flag {
            width: 30px;
            height: 20px;
            border-radius: 4px;
            margin-right: 10px;
            object-fit: cover;
        }

        .office-city {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }

        .office-city-name {
            font-size: 1.5rem;
            font-weight: 700;
            margin: 0;
            color: var(--text-light);
        }

        .office-address {
            color: var(--text-muted);
            font-size: 1.05rem;
            line-height: 1.6;
            margin-bottom: 15px;
        }

        .office-contact {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }

        .office-contact i {
            color: var(--color-7);
            margin-right: 10px;
            font-size: 1.1rem;
        }

        .office-contact a {
            color: var(--text-light);
            text-decoration: none;
            transition: var(--transition);
        }

        .office-contact a:hover {
            color: var(--color-8);
        }

        /* FAQ SECTION */
        .faq-section {
            padding: 80px 0;
            position: relative;
        }

        .accordion-item {
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
            border-radius: 15px !important;
            margin-bottom: 20px;
            overflow: hidden;
        }

        .accordion-header {
            margin: 0;
        }

        .accordion-button {
            background: transparent !important;
            color: var(--text-light) !important;
            font-size: 1.2rem;
            font-weight: 600;
            padding: 25px 30px;
            box-shadow: none !important;
            font-family: var(--heading-font);
        }

        .accordion-button:not(.collapsed) {
            color: var(--color-8) !important;
        }

        .accordion-button::after {
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 16 16' fill='%23ffffff'%3e%3cpath fill-rule='evenodd' d='M1.646 4.646a.5.5 0 0 1 .708 0L8 10.293l5.646-5.647a.5.5 0 0 1 .708.708l-6 6a.5.5 0 0 1-.708 0l-6-6a.5.5 0 0 1 0-.708z'/%3e%3c/svg%3e") !important;
        }

        .accordion-body {
            padding: 0 30px 25px 30px;
            color: var(--text-muted);
            font-size: 1.1rem;
            line-height: 1.7;
        }

        html.light-mode .accordion-button::after {
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 16 16' fill='%23000000'%3e%3cpath fill-rule='evenodd' d='M1.646 4.646a.5.5 0 0 1 .708 0L8 10.293l5.646-5.647a.5.5 0 0 1 .708.708l-6 6a.5.5 0 0 1-.708 0l-6-6a.5.5 0 0 1 0-.708z'/%3e%3c/svg%3e") !important;
        }

        /* FOOTER */
        .footer {
            background: linear-gradient(135deg, var(--dark-bg) 0%, #190040 50%, var(--dark-bg) 100%);
            padding: 60px 0 20px;
            position: relative;
            margin-top: 60px;
        }

        /* Light Mode Footer */
        html.light-mode .footer {
            background: linear-gradient(135deg, #f5f5f7 0%, #e8e8ea 50%, #f5f5f7 100%);
        }

        .footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--color-7), transparent);
        }

        /* Light Mode Footer Border */
        html.light-mode .footer::before {
            background: linear-gradient(90deg, transparent, rgba(106, 75, 165, 0.3), transparent);
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .footer-main {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 400px;
            gap: 40px;
            margin-bottom: 40px;
            align-items: start;
        }

        .footer-logo-section {
            padding-right: 20px;
        }

        .footer-description {
            color: var(--text-muted);
            margin-bottom: 25px;
            font-size: 1rem;
            line-height: 1.8;
        }

        .footer-social {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }

        .footer-social a {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--text-light);
            text-decoration: none;
            font-size: 1.2rem;
            transition: var(--transition);
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
        }

        /* Light Mode Footer Social */
        html.light-mode .footer-social a {
            background: rgba(255, 255, 255, 0.8);
            border: 1px solid rgba(106, 75, 165, 0.15);
        }

        .footer-social a:hover {
            background: var(--gradient-3);
            color: var(--text-dark);
            transform: translateY(-5px);
        }

        /* Light Mode Footer Social Hover */
        html.light-mode .footer-social a:hover {
            color: #ffffff;
        }

        .footer-links-section h6 {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 25px;
            color: var(--text-light);
            position: relative;
            padding-bottom: 10px;
        }

        .footer-links-section h6::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 40px;
            height: 2px;
            background: var(--gradient-3);
        }

        .footer-links-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .footer-links-list li {
            margin-bottom: 12px;
        }

        .footer-links-list a {
            color: var(--text-muted);
            text-decoration: none;
            transition: var(--transition);
            display: inline-block;
            font-size: 1rem;
        }

        .footer-links-list a:hover {
            color: var(--color-7);
            transform: translateX(5px);
        }

        .footer-newsletter {
            background: var(--glass-bg);
            backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 30px;
            box-shadow: var(--glass-shadow);
        }

        /* Light Mode Footer Newsletter */
        html.light-mode .footer-newsletter {
            background: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(106, 75, 165, 0.1);
            box-shadow: 0 8px 32px rgba(106, 75, 165, 0.1);
        }

        .footer-newsletter h6 {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 15px;
            color: var(--text-light);
            position: relative;
            padding-bottom: 10px;
        }

        .footer-newsletter h6::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 40px;
            height: 2px;
            background: var(--gradient-3);
        }

        .footer-newsletter p {
            color: var(--text-muted);
            font-size: 0.95rem;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .footer-newsletter-form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .footer-newsletter-form input {
            padding: 12px 16px;
            border-radius: 25px;
            border: 1px solid var(--glass-border);
            background: var(--glass-bg);
            color: var(--text-light);
            font-size: 1rem;
            transition: var(--transition);
        }

        /* Light Mode Newsletter Input */
        html.light-mode .footer-newsletter-form input {
            background: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(106, 75, 165, 0.2);
        }

        .footer-newsletter-form input::placeholder {
            color: var(--text-muted);
        }

        .footer-newsletter-form input:focus {
            outline: none;
            border-color: var(--color-6);
            box-shadow: 0 0 10px rgba(72, 191, 227, 0.4);
        }

        /* Light Mode Newsletter Input Focus */
        html.light-mode .footer-newsletter-form input:focus {
            box-shadow: 0 0 10px rgba(106, 75, 165, 0.3);
        }

        .footer-newsletter-form button {
            padding: 12px 24px;
            border-radius: 25px;
            border: none;
            background: var(--gradient-3);
            color: var(--text-dark);
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            font-size: 1rem;
        }

        .footer-newsletter-form button:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(72, 191, 227, 0.4);
        }

        /* Light Mode Newsletter Button */
        html.light-mode .footer-newsletter-form button {
            color: #ffffff;
        }

        html.light-mode .footer-newsletter-form button:hover {
            color: #ffffff;
        }

        .footer-divider {
            height: 1px;
            background: var(--glass-border);
            margin: 40px 0 30px;
        }

        /* Light Mode Footer Divider */
        html.light-mode .footer-divider {
            background: rgba(106, 75, 165, 0.15);
        }

        .footer-bottom {
            color: var(--text-muted);
            font-size: 0.95rem;
            font-family: var(--body-font);
        }
        
        .footer-bottom a {
            color: var(--color-8);
            transition: var(--transition);
        }
        
        .footer-bottom a:hover {
            color: var(--color-9);
        }

        /* PROGRESS BAR */
        .progress-bar {
            position: fixed;
            top: 0;
            left: 0;
            width: 0%;
            height: 4px;
            background: var(--gradient-5);
            z-index: 1050;
            transition: width 0.1s linear;
            box-shadow: 0 0 10px rgba(116, 0, 184, 0.5);
        }

        /* Light Mode Progress Bar */
        html.light-mode .progress-bar {
            box-shadow: 0 0 8px rgba(106, 75, 165, 0.3);
        }

        /* CUSTOM CURSOR */
        .cursor,
        .cursor-follower {
            position: fixed;
            pointer-events: none;
            z-index: 9999;
            border-radius: 50%;
            transition: all 0.1s ease;
        }

        .cursor {
            width: 20px;
            height: 20px;
            background-color: rgba(128, 255, 219, 0.8);
            mix-blend-mode: difference;
            transform: translate(-50%, -50%);
        }

        .cursor-follower {
            width: 40px;
            height: 40px;
            border: 2px solid rgba(128, 255, 219, 0.5);
            transform: translate(-50%, -50%);
        }

        .cursor.active {
            width: 40px;
            height: 40px;
            background-color: rgba(116, 0, 184, 0.8);
        }

        .cursor-follower.active {
            width: 80px;
            height: 80px;
            border-color: rgba(116, 0, 184, 0.5);
        }

        /* Light Mode Cursor Adjustments */
        html.light-mode .cursor {
            background-color: rgba(106, 75, 165, 0.7);
            mix-blend-mode: multiply;
        }

        html.light-mode .cursor-follower {
            border-color: rgba(106, 75, 165, 0.4);
        }

        html.light-mode .cursor.active {
            background-color: rgba(106, 75, 165, 0.8);
        }

        html.light-mode .cursor-follower.active {
            border-color: rgba(106, 75, 165, 0.6);
        }

        /* LANGUAGE HANDLING */
        .vi, .en {
            display: none;
        }

        html[lang="vi"] .vi {
            display: block;
        }

        html[lang="en"] .en {
            display: block;
        }

        span.vi, span.en {
            display: none;
        }

        html[lang="vi"] span.vi {
            display: inline;
        }

        html[lang="en"] span.en {
            display: inline;
        }
        
        /* Additional label handling for form elements */
        label.vi, label.en {
            display: none;
        }
        
        html[lang="vi"] label.vi {
            display: block;
        }
        
        html[lang="en"] label.en {
            display: block;
        }

        /* ANIMATIONS */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.6s ease;
            will-change: opacity, transform;
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* LOADING STATES */
        .form-loading {
            position: relative;
            pointer-events: none;
        }

        .form-loading::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(2px);
            border-radius: 30px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .form-loading::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 40px;
            height: 40px;
            border: 4px solid var(--color-7);
            border-radius: 50%;
            border-top-color: transparent;
            animation: spin 1s linear infinite;
            z-index: 2;
        }

        @keyframes spin {
            to { transform: translate(-50%, -50%) rotate(360deg); }
        }

        /* SUCCESS/ERROR MESSAGES */
        .message {
            padding: 20px;
            border-radius: 20px;
            margin-bottom: 25px;
            font-weight: 500;
            display: none;
            animation: slideDown 0.3s ease;
        }

        .message.success {
            background: rgba(34, 197, 94, 0.1);
            border: 1px solid rgba(34, 197, 94, 0.3);
            color: #22c55e;
        }

        .message.error {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.3);
            color: #ef4444;
        }

        .message.show {
            display: block;
        }

        /* Light Mode Messages */
        html.light-mode .message.success {
            background: rgba(34, 197, 94, 0.1);
            border: 1px solid rgba(34, 197, 94, 0.2);
            color: #15803d;
        }

        html.light-mode .message.error {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.2);
            color: #dc2626;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* RESPONSIVE */
        @media (max-width: 768px) {
            .hero-title,
            .hero-title .main-text {
                font-size: 2.5rem;
            }
            .section-title {
                font-size: 2rem;
            }
            .contact-form-section {
                padding: 30px;
            }
            .contact-info-card {
                padding: 30px;
                flex-direction: column;
                text-align: center;
            }
            .contact-info-icon {
                margin-right: 0;
                margin-bottom: 20px;
            }
            .footer-main {
                grid-template-columns: 1fr;
                gap: 30px;
            }
            .footer-bottom {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }
            .scroll-to-top {
                bottom: 20px;
                left: 20px;
                width: 45px;
                height: 45px;
            }
            .theme-switch {
                bottom: 20px;
                right: 20px;
                width: 50px;
                height: 50px;
            }
            .language-switch {
                bottom: 80px;
                right: 20px;
                width: 50px;
                height: 50px;
            }
            .cursor, .cursor-follower {
                display: none !important;
            }
            /* Disable expensive animations on mobile */
            .contact-info-card,
            .contact-form-section {
                transition: none;
            }
            body::before {
                animation: none;
                transform: none;
            }
            .map-overlay {
                position: relative;
                top: auto;
                left: auto;
                max-width: 100%;
                margin-bottom: 20px;
            }
            .office-card-header {
                flex-direction: column;
                text-align: center;
            }
            .office-flag {
                margin-bottom: 10px;
            }
        }

        /* SCROLL BEHAVIOR */
        html {
            scroll-behavior: smooth;
        }

        body {
            scroll-behavior: smooth;
        }

        /* OPTIMIZATION: Pause animations when not in view */
        .logo-container:not(.in-view) .logo-circle,
        .logo-container:not(.in-view) .orbiting-dot {
            animation-play-state: paused;
        }

        .logo-container.in-view .logo-circle,
        .logo-container.in-view .orbiting-dot {
            animation-play-state: running;
        }
    </style>
</head>

<body>
    <!-- Loader with SOFIN Logo -->
    <div class="loader-wrapper">
        <div class="loader-logo-container">
            <div class="loader-logo-circle loader-circle-1"></div>
            <div class="loader-logo-circle loader-circle-2"></div>
            <div class="loader-logo-circle loader-circle-3"></div>
            <div class="loader-logo-circle loader-circle-4"></div>

            <div class="loader-dots-container" id="loader-dots"></div>
            <div class="loader-orbiting-dot" id="loader-orbit-1"></div>
            <div class="loader-orbiting-dot" id="loader-orbit-2"></div>
            <div class="loader-orbiting-dot" id="loader-orbit-3"></div>

            <div class="loader-logo-text">
                <div class="loader-logo-main">SOFIN</div>
                <div class="loader-logo-tagline">NETWORK</div>
            </div>
        </div>
        <div class="loader"></div>
    </div>

    <!-- Custom Cursor -->
    <div class="cursor"></div>
    <div class="cursor-follower"></div>

    <!-- Progress Bar -->
    <div class="progress-bar"></div>

    <!-- Theme Switch -->
    <div class="theme-switch" id="theme-switch">
        <i class="fas fa-sun"></i>
    </div>

    <!-- Language Switch -->
    <div class="language-switch" id="language-switch">
        <span class="current-lang">VI</span>
    </div>

    <!-- Scroll to Top Button -->
    <button class="scroll-to-top" id="scroll-to-top">
        <i class="fas fa-arrow-up"></i>
    </button>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="<?=set_route_to_link([])?>">
                <div class="logo-container">
                    <!-- <div class="logo-circle circle-1"></div>
                    <div class="logo-circle circle-2"></div>
                    <div class="logo-circle circle-3"></div> -->
                    <div class="logo-dots" id="logo-dots"></div>
                    <div class="orbiting-dot" id="orbit-dot-1"></div>
                    <div class="orbiting-dot" id="orbit-dot-2"></div>
                    <div class="logo-text">
                        <div class="logo-main">SOFIN</div>
                        <div class="logo-tagline">NETWORK</div>
                    </div>
                </div>
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <?=$menus?>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="hero-overlay"></div>
        <div class="container">
            <div class="hero-content">
                <p class="hero-subtitle vi">Hãy Liên Hệ Với Chúng Tôi</p>
                <p class="hero-subtitle en">Get In Touch With Us</p>

                <h1 class="hero-title">
                    <div class="main-text vi">Liên Hệ</div>
                    <div class="main-text en">Contact</div>
                </h1>

                <p class="hero-description vi">
                    Chúng tôi luôn sẵn sàng lắng nghe và hỗ trợ bạn. Hãy liên hệ để khám phá những giải pháp AI tuyệt vời nhất dành cho doanh nghiệp của bạn.
                </p>
                <p class="hero-description en">
                    We are always ready to listen and support you. Contact us to explore the best AI solutions for your business.
                </p>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="contact-section">
        <div class="container">
            <!-- Section Header -->
            <div class="section-header text-center">
                <h2 class="section-title vi">Thông Tin Liên Hệ</h2>
                <h2 class="section-title en">Contact Information</h2>
                <p class="section-subtitle vi">Hãy liên hệ với SOFIN NETWORK để được hỗ trợ và tư vấn về các dịch vụ AI, đào tạo, hợp tác kinh doanh</p>
                <p class="section-subtitle en">Contact SOFIN NETWORK for support and consultation on AI services, training, business cooperation</p>
            </div>

            <!-- Contact Info Cards -->
            <div class="contact-info-cards">
                <div class="row">
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="contact-info-card reveal">
                            <div class="contact-info-icon">
                                <i class="fas fa-building"></i>
                            </div>
                            <div class="contact-info-content">
                                <h5 class="vi">Trụ Sở Chính</h5>
                                <h5 class="en">Main Office</h5>
                                <p class="vi">126-128 Trần Hưng Đạo, Phường An Hải Tây, Quận Sơn Trà, Đà Nẵng, Việt Nam</p>
                                <p class="en">126-128 Tran Hung Dao, An Hai Tay Ward, Son Tra District, Da Nang, Vietnam</p>
                                <div class="contact-social">
                                    <a href="#"><i class="fab fa-facebook-f"></i></a>
                                    <a href="#"><i class="fab fa-linkedin-in"></i></a>
                                    <a href="#"><i class="fab fa-twitter"></i></a>
                                    <a href="#"><i class="fab fa-instagram"></i></a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="contact-info-card reveal">
                            <div class="contact-info-icon">
                                <i class="fas fa-headset"></i>
                            </div>
                            <div class="contact-info-content">
                                <h5 class="vi">Hỗ Trợ</h5>
                                <h5 class="en">Support</h5>
                                <p><i class="fas fa-phone-alt"></i> 
                                    <a href="tel:+842363888999">+84 236 3888 999</a>
                                </p>
                                <p><i class="fas fa-envelope"></i> 
                                    <a href="mailto:support@sofinnetwork.com">support@sofinnetwork.com</a>
                                </p>
                                <p class="vi"><i class="fas fa-clock"></i> 24/7 Hỗ trợ</p>
                                <p class="en"><i class="fas fa-clock"></i> 24/7 Support</p>
                            </div>
                        </div>
                    </div>

                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="contact-info-card reveal">
                            <div class="contact-info-icon">
                                <i class="fas fa-graduation-cap"></i>
                            </div>
                            <div class="contact-info-content">
                                <h5 class="vi">Đào Tạo</h5>
                                <h5 class="en">Training</h5>
                                <p><i class="fas fa-phone-alt"></i> 
                                    <a href="tel:+842363888902">+84 236 3888 902</a>
                                </p>
                                <p><i class="fas fa-envelope"></i> 
                                    <a href="mailto:training@sofinnetwork.com">training@sofinnetwork.com</a>
                                </p>
                                <p class="vi"><i class="fas fa-clock"></i> Thứ Hai - Thứ Sáu: 8:00 - 17:30</p>
                                <p class="en"><i class="fas fa-clock"></i> Monday - Friday: 8:00 - 17:30</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Contact Form and Map Row -->
            <div class="row">
                <!-- Contact Form Section -->
                <div class="col-lg-8">
                    <div class="contact-form-section reveal">
                        <div class="contact-form-header">
                            <h2 class="contact-form-title vi">Gửi Tin Nhắn</h2>
                            <h2 class="contact-form-title en">Send Message</h2>
                            <p class="contact-form-subtitle vi">
                                Điền thông tin bên dưới và chúng tôi sẽ liên hệ lại với bạn trong thời gian sớm nhất.
                            </p>
                            <p class="contact-form-subtitle en">
                                Fill in the information below and we will contact you as soon as possible.
                            </p>
                        </div>

                        <div id="message" class="message"></div>

                        <form id="contactForm">
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="fullName" class="vi">Họ và Tên *</label>
                                        <label for="fullName" class="en">Full Name *</label>
                                        <input type="text" class="form-control" id="fullName" name="fullName" 
                                               placeholder="Nhập họ và tên của bạn" data-vi-placeholder="Nhập họ và tên của bạn" data-en-placeholder="Enter your full name" required>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="email" class="vi">Email *</label>
                                        <label for="email" class="en">Email *</label>
                                        <input type="email" class="form-control" id="email" name="email" 
                                               placeholder="example@email.com" data-vi-placeholder="example@email.com" data-en-placeholder="example@email.com" required>
                                    </div>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="phone" class="vi">Số Điện Thoại</label>
                                        <label for="phone" class="en">Phone Number</label>
                                        <input type="tel" class="form-control" id="phone" name="phone" 
                                               placeholder="+84 123 456 789" data-vi-placeholder="+84 123 456 789" data-en-placeholder="+84 123 456 789">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="company" class="vi">Công Ty</label>
                                        <label for="company" class="en">Company</label>
                                        <input type="text" class="form-control" id="company" name="company" 
                                               placeholder="Tên công ty" data-vi-placeholder="Tên công ty" data-en-placeholder="Company name">
                                    </div>
                                </div>
                            </div>
                            <div class="form-group">
                                <label for="subject" class="vi">Chủ Đề *</label>
                                <label for="subject" class="en">Subject *</label>
                                <input type="text" class="form-control" id="subject" name="subject" 
                                       placeholder="Chủ đề tin nhắn" data-vi-placeholder="Chủ đề tin nhắn" data-en-placeholder="Message subject" required>
                            </div>
                            <div class="form-group">
                                <label for="messageText" class="vi">Tin Nhắn *</label>
                                <label for="messageText" class="en">Message *</label>
                                <textarea class="form-control" id="messageText" name="message" rows="6" 
                                          placeholder="Nhập nội dung tin nhắn của bạn tại đây..." 
                                          data-vi-placeholder="Nhập nội dung tin nhắn của bạn tại đây..."
                                          data-en-placeholder="Enter your message here..." required></textarea>
                            </div>
                            <div class="text-center">
                                <button type="submit" class="btn-primary-custom">
                                    <i class="fas fa-paper-plane"></i>
                                    <span class="vi">Gửi Tin Nhắn</span>
                                    <span class="en">Send Message</span>
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- Map Section -->
                <div class="col-lg-4">
                    <div class="map-container reveal">
                        <div class="map-overlay">
                            <h3 class="map-card-title vi">Vị Trí</h3>
                            <h3 class="map-card-title en">Location</h3>
                            <p class="map-card-address">126-128 Trần Hưng Đạo, Phường An Hải Tây, Quận Sơn Trà, Đà Nẵng, Việt Nam</p>
                        </div>
                        <div class="map-wrapper">
                            <iframe 
                                src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3834.199000561854!2d108.22975007592187!3d16.055159939812878!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x314219d6da7a5209%3A0x2462f0ff1ec118c!2zMTI2IMSQLiBUcuG6p24gSMawbmcgxJDhuqFvLCBBbiBI4bqjaSBUw6J5LCBTxqFuIFRyw6AsIMSQw6AgTuG6tW5nIDU1MDAwMCwgVmnhu4d0IE5hbQ!5e0!3m2!1svi!2sus!4v1747129990346!5m2!1svi!2sus" 
                                allowfullscreen="" 
                                loading="lazy" 
                                referrerpolicy="no-referrer-when-downgrade">
                            </iframe>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Global Offices Section -->
            <div class="contact-offices">
                <div class="section-header text-center">
                    <h2 class="section-title vi">Văn Phòng Toàn Cầu</h2>
                    <h2 class="section-title en">Global Offices</h2>
                    <p class="section-subtitle vi">SOFIN NETWORK có mặt trên toàn cầu để hỗ trợ khách hàng tại địa phương</p>
                    <p class="section-subtitle en">SOFIN NETWORK is present globally to support local customers</p>
                </div>

                <div class="row">
                    <!-- Office 1 - Ho Chi Minh -->
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="office-card reveal">
                            <div class="office-city">
                                <img src="https://flagcdn.com/w40/vn.png" alt="Vietnam" class="office-flag">
                                <h4 class="office-city-name">Hồ Chí Minh</h4>
                            </div>
                            <p class="office-address">Tầng 15, Tòa nhà Viettel, 285 Cách Mạng Tháng Tám, Phường 12, Quận 10, TP. Hồ Chí Minh</p>
                            <div class="office-contact">
                                <i class="fas fa-phone-alt"></i>
                                <a href="tel:+84283888999">+84 28 3888 999</a>
                            </div>
                            <div class="office-contact">
                                <i class="fas fa-envelope"></i>
                                <a href="mailto:hcm@sofinnetwork.com">hcm@sofinnetwork.com</a>
                            </div>
                        </div>
                    </div>

                    <!-- Office 2 - Hanoi -->
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="office-card reveal">
                            <div class="office-city">
                                <img src="https://flagcdn.com/w40/vn.png" alt="Vietnam" class="office-flag">
                                <h4 class="office-city-name">Hà Nội</h4>
                            </div>
                            <p class="office-address">Tầng 9, Tòa nhà TNR, 54A Nguyễn Chí Thanh, Đống Đa, Hà Nội</p>
                            <div class="office-contact">
                                <i class="fas fa-phone-alt"></i>
                                <a href="tel:+84243888999">+84 24 3888 999</a>
                            </div>
                            <div class="office-contact">
                                <i class="fas fa-envelope"></i>
                                <a href="mailto:hanoi@sofinnetwork.com">hanoi@sofinnetwork.com</a>
                            </div>
                        </div>
                    </div>

                    <!-- Office 3 - Singapore -->
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="office-card reveal">
                            <div class="office-city">
                                <img src="https://flagcdn.com/w40/sg.png" alt="Singapore" class="office-flag">
                                <h4 class="office-city-name">Singapore</h4>
                            </div>
                            <p class="office-address">Level 18, Mapletree Business City, 20 Pasir Panjang Road, Singapore 117439</p>
                            <div class="office-contact">
                                <i class="fas fa-phone-alt"></i>
                                <a href="tel:+6565888999">+65 6588 8999</a>
                            </div>
                            <div class="office-contact">
                                <i class="fas fa-envelope"></i>
                                <a href="mailto:singapore@sofinnetwork.com">singapore@sofinnetwork.com</a>
                            </div>
                        </div>
                    </div>

                    <!-- Office 4 - Tokyo -->
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="office-card reveal">
                            <div class="office-city">
                                <img src="https://flagcdn.com/w40/jp.png" alt="Japan" class="office-flag">
                                <h4 class="office-city-name">Tokyo</h4>
                            </div>
                            <p class="office-address">Sumitomo Fudosan Roppongi Grand Tower 25F, 3-2-1 Roppongi, Minato-ku, Tokyo, 106-6224, Japan</p>
                            <div class="office-contact">
                                <i class="fas fa-phone-alt"></i>
                                <a href="tel:+81363888999">+81 3 6388 8999</a>
                            </div>
                            <div class="office-contact">
                                <i class="fas fa-envelope"></i>
                                <a href="mailto:tokyo@sofinnetwork.com">tokyo@sofinnetwork.com</a>
                            </div>
                        </div>
                    </div>

                    <!-- Office 5 - San Francisco -->
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="office-card reveal">
                            <div class="office-city">
                                <img src="https://flagcdn.com/w40/us.png" alt="USA" class="office-flag">
                                <h4 class="office-city-name">San Francisco</h4>
                            </div>
                            <p class="office-address">555 California Street, Suite 4925, San Francisco, CA 94104, USA</p>
                            <div class="office-contact">
                                <i class="fas fa-phone-alt"></i>
                                <a href="tel:+14153888999">+1 415 388 8999</a>
                            </div>
                            <div class="office-contact">
                                <i class="fas fa-envelope"></i>
                                <a href="mailto:sf@sofinnetwork.com">sf@sofinnetwork.com</a>
                            </div>
                        </div>
                    </div>

                    <!-- Office 6 - London -->
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="office-card reveal">
                            <div class="office-city">
                                <img src="https://flagcdn.com/w40/gb.png" alt="UK" class="office-flag">
                                <h4 class="office-city-name">London</h4>
                            </div>
                            <p class="office-address">Level 33, 25 Canada Square, Canary Wharf, London E14 5LQ, United Kingdom</p>
                            <div class="office-contact">
                                <i class="fas fa-phone-alt"></i>
                                <a href="tel:+442038888999">+44 20 3888 8999</a>
                            </div>
                            <div class="office-contact">
                                <i class="fas fa-envelope"></i>
                                <a href="mailto:london@sofinnetwork.com">london@sofinnetwork.com</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq-section">
        <div class="container">
            <div class="section-header text-center">
                <h2 class="section-title vi">Câu Hỏi Thường Gặp</h2>
                <h2 class="section-title en">Frequently Asked Questions</h2>
                <p class="section-subtitle vi">Những câu hỏi thường gặp về dịch vụ của SOFIN NETWORK</p>
                <p class="section-subtitle en">Frequently asked questions about SOFIN NETWORK services</p>
            </div>

            <div class="accordion" id="faqAccordion">
                <!-- FAQ Item 1 -->
                <div class="accordion-item">
                    <h2 class="accordion-header" id="headingOne">
                        <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                            <span class="vi">SOFIN NETWORK cung cấp những dịch vụ nào?</span>
                            <span class="en">What services does SOFIN NETWORK provide?</span>
                        </button>
                    </h2>
                    <div id="collapseOne" class="accordion-collapse collapse show" aria-labelledby="headingOne" data-bs-parent="#faqAccordion">
                        <div class="accordion-body">
                            <p class="vi">SOFIN NETWORK cung cấp nhiều dịch vụ về AI và công nghệ bao gồm: phát triển giải pháp AI tùy chỉnh, đào tạo và tư vấn về AI, phân phối nội dung số, các giải pháp tự động hóa thông minh cho doanh nghiệp, và các dịch vụ hợp tác chiến lược.</p>
                            <p class="en">SOFIN NETWORK provides many AI and technology services including: custom AI solution development, AI training and consulting, digital content distribution, intelligent automation solutions for businesses, and strategic partnership services.</p>
                        </div>
                    </div>
                </div>

                <!-- FAQ Item 2 -->
                <div class="accordion-item">
                    <h2 class="accordion-header" id="headingTwo">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
                            <span class="vi">Làm thế nào để bắt đầu hợp tác với SOFIN NETWORK?</span>
                            <span class="en">How to start working with SOFIN NETWORK?</span>
                        </button>
                    </h2>
                    <div id="collapseTwo" class="accordion-collapse collapse" aria-labelledby="headingTwo" data-bs-parent="#faqAccordion">
                        <div class="accordion-body">
                            <p class="vi">Để bắt đầu hợp tác với SOFIN NETWORK, bạn có thể liên hệ với chúng tôi qua email, điện thoại hoặc biểu mẫu liên hệ trên trang web. Sau khi nhận được yêu cầu, đội ngũ tư vấn của chúng tôi sẽ sắp xếp một cuộc họp để tìm hiểu nhu cầu cụ thể của bạn và đề xuất giải pháp phù hợp nhất.</p>
                            <p class="en">To start working with SOFIN NETWORK, you can contact us via email, phone, or the contact form on the website. After receiving your request, our consulting team will arrange a meeting to understand your specific needs and propose the most suitable solution.</p>
                        </div>
                    </div>
                </div>

                <!-- FAQ Item 3 -->
                <div class="accordion-item">
                    <h2 class="accordion-header" id="headingThree">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseThree" aria-expanded="false" aria-controls="collapseThree">
                            <span class="vi">SOFIN NETWORK có cung cấp dịch vụ cho khách hàng quốc tế không?</span>
                            <span class="en">Does SOFIN NETWORK provide services to international customers?</span>
                        </button>
                    </h2>
                    <div id="collapseThree" class="accordion-collapse collapse" aria-labelledby="headingThree" data-bs-parent="#faqAccordion">
                        <div class="accordion-body">
                            <p class="vi">Có, SOFIN NETWORK phục vụ cả khách hàng trong nước và quốc tế. Chúng tôi có các văn phòng đại diện tại nhiều quốc gia và đội ngũ nhân viên đa ngôn ngữ để hỗ trợ khách hàng từ khắp nơi trên thế giới. Các giải pháp của chúng tôi được thiết kế để đáp ứng tiêu chuẩn quốc tế và yêu cầu riêng của từng thị trường.</p>
                            <p class="en">Yes, SOFIN NETWORK serves both domestic and international customers. We have representative offices in many countries and a multilingual staff to support customers from all over the world. Our solutions are designed to meet international standards and the specific requirements of each market.</p>
                        </div>
                    </div>
                </div>

                <!-- FAQ Item 4 -->
                <div class="accordion-item">
                    <h2 class="accordion-header" id="headingFour">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseFour" aria-expanded="false" aria-controls="collapseFour">
                            <span class="vi">Các khóa đào tạo AI của SOFIN NETWORK phù hợp với đối tượng nào?</span>
                            <span class="en">Who are SOFIN NETWORK's AI training courses suitable for?</span>
                        </button>
                    </h2>
                    <div id="collapseFour" class="accordion-collapse collapse" aria-labelledby="headingFour" data-bs-parent="#faqAccordion">
                        <div class="accordion-body">
                            <p class="vi">Các khóa đào tạo AI của SOFIN NETWORK phù hợp với nhiều đối tượng khác nhau, từ người mới bắt đầu đến chuyên gia. Chúng tôi cung cấp các khóa học cho cá nhân muốn nâng cao kỹ năng, nhóm doanh nghiệp cần đào tạo nội bộ về AI, và các tổ chức giáo dục. Nội dung đào tạo được thiết kế linh hoạt và tùy chỉnh theo nhu cầu cụ thể của từng đối tượng học viên.</p>
                            <p class="en">SOFIN NETWORK's AI training courses are suitable for various audiences, from beginners to experts. We provide courses for individuals looking to enhance their skills, business groups needing internal AI training, and educational organizations. Training content is designed flexibly and customized according to the specific needs of each student group.</p>
                        </div>
                    </div>
                </div>

                <!-- FAQ Item 5 -->
                <div class="accordion-item">
                    <h2 class="accordion-header" id="headingFive">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseFive" aria-expanded="false" aria-controls="collapseFive">
                            <span class="vi">Làm thế nào để theo dõi tin tức và cập nhật mới nhất từ SOFIN NETWORK?</span>
                            <span class="en">How to follow news and latest updates from SOFIN NETWORK?</span>
                        </button>
                    </h2>
                    <div id="collapseFive" class="accordion-collapse collapse" aria-labelledby="headingFive" data-bs-parent="#faqAccordion">
                        <div class="accordion-body">
                            <p class="vi">Bạn có thể theo dõi tin tức và cập nhật mới nhất từ SOFIN NETWORK qua trang web chính thức của chúng tôi, đăng ký nhận bản tin qua email, hoặc theo dõi các kênh mạng xã hội bao gồm Facebook, LinkedIn, Twitter và Instagram. Chúng tôi thường xuyên chia sẻ thông tin về sản phẩm mới, công nghệ AI tiên tiến, và các sự kiện sắp tới.</p>
                            <p class="en">You can follow the latest news and updates from SOFIN NETWORK through our official website, subscribe to our email newsletter, or follow our social media channels including Facebook, LinkedIn, Twitter, and Instagram. We regularly share information about new products, advanced AI technologies, and upcoming events.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer" id="footer">
        <div class="footer-content">
            <div class="footer-main">
                <!-- Logo and Description -->
                <div class="footer-logo-section">
                    <div class="footer-logo-container">
                        <div class="logo-container" style="transform: scale(0.9); margin-left: -15px; margin-bottom: 20px;">
                            <div class="logo-circle circle-1"></div>
                            <div class="logo-circle circle-2"></div>
                            <div class="logo-circle circle-3"></div>
                            <div class="logo-dots footer-logo-dots"></div>
                            <div class="orbiting-dot footer-orbit-dot-1"></div>
                            <div class="orbiting-dot footer-orbit-dot-2"></div>
                            <div class="logo-text">
                                <div class="logo-main">SOFIN</div>
                                <div class="logo-tagline">NETWORK</div>
                            </div>
                        </div>
                    </div>
                    <p class="footer-description vi">
                        SOFIN NETWORK là đơn vị tiên phong trong lĩnh vực phát triển và ứng dụng công nghệ AI vào hoạt động kinh doanh. Chúng tôi cam kết mang đến các giải pháp thông minh, hiệu quả và phù hợp với nhu cầu thực tế của doanh nghiệp Việt Nam.
                    </p>
                    <p class="footer-description en">
                        SOFIN NETWORK is a pioneer in developing and applying AI technology to business operations. We are committed to providing smart, efficient solutions tailored to the practical needs of music and entertainment businesses.
                    </p>
                    <div class="footer-social">
                        <a href="#" aria-label="Facebook">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                        <a href="#" aria-label="LinkedIn">
                            <i class="fab fa-linkedin-in"></i>
                        </a>
                        <a href="#" aria-label="YouTube">
                            <i class="fab fa-youtube"></i>
                        </a>
                        <a href="#" aria-label="Instagram">
                            <i class="fab fa-instagram"></i>
                        </a>
                    </div>
                </div>

                <!-- Links -->
                <div class="footer-links-section">
                    <h6 class="vi">Liên Kết</h6>
                    <h6 class="en">Links</h6>
                    <ul class="footer-links-list">
                        <li>
                            <a href="../index.php">
                                <span class="vi">Trang Chủ</span>
                                <span class="en">Home</span>
                            </a>
                        </li>
                        <li>
                            <a href="../page2/sofin_ai.php">
                                <span class="vi">SOFIN AI</span>
                                <span class="en">SOFIN AI</span>
                            </a>
                        </li>
                        <li>
                            <a href="../page2/education.php">
                                <span class="vi">Đào Tạo</span>
                                <span class="en">Training</span>
                            </a>
                        </li>
                        <li>
                            <a href="../page2/content_distribution.php">
                                <span class="vi">Phân Phối Nội Dung</span>
                                <span class="en">Content Distribution</span>
                            </a>
                        </li>
                        <li>
                            <a href="../page2/connect.php">
                                <span class="vi">Kết Nối</span>
                                <span class="en">Connect</span>
                            </a>
                        </li>
                        <li>
                            <a href="../page2/contact.php">
                                <span class="vi">Liên Hệ</span>
                                <span class="en">Contact</span>
                            </a>
                        </li>
                    </ul>
                </div>

                <!-- Services -->
                <div class="footer-links-section">
                    <h6 class="vi">Dịch Vụ</h6>
                    <h6 class="en">Services</h6>
                    <ul class="footer-links-list">
                        <li>
                            <a href="#">
                                <span class="vi">Tư Vấn AI</span>
                                <span class="en">AI Consulting</span>
                            </a>
                        </li>
                        <li>
                            <a href="#">
                                <span class="vi">Phát Triển Giải Pháp</span>
                                <span class="en">Solution Development</span>
                            </a>
                        </li>
                        <li>
                            <a href="#">
                                <span class="vi">Đào Tạo Nhân Lực</span>
                                <span class="en">Workforce Training</span>
                            </a>
                        </li>
                        <li>
                            <a href="#">
                                <span class="vi">Nghiên Cứu Thị Trường</span>
                                <span class="en">Market Research</span>
                            </a>
                        </li>
                        <li>
                            <a href="#">
                                <span class="vi">Tích Hợp Hệ Thống</span>
                                <span class="en">System Integration</span>
                            </a>
                        </li>
                    </ul>
                </div>

                <!-- Newsletter -->
                <div class="footer-newsletter">
                    <h6 class="vi">Đăng Ký Nhận Tin</h6>
                    <h6 class="en">Subscribe Newsletter</h6>
                    <p class="vi">
                        Đăng ký để nhận thông tin mới nhất về công nghệ AI, sự kiện và các giải pháp mới từ SOFIN NETWORK.
                    </p>
                    <p class="en">
                        Subscribe to receive the latest information on AI technology, events and new solutions from SOFIN NETWORK.
                    </p>
                    <div class="footer-newsletter-form">
                        <input type="email" placeholder="Email của bạn" data-en-placeholder="Your email">
                        <button type="submit">
                            <span class="vi">ĐĂNG KÝ</span>
                            <span class="en">SUBSCRIBE</span>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Bottom row -->
            <div class="footer-divider"></div>
            <div class="footer-bottom">
                <div class="row">
                    <div class="col-md-6">
                        <p>© 2025 SOFIN NETWORK. <span class="vi">Đã đăng ký bản quyền. Thiết kế bởi</span><span class="en">All rights reserved. Designed by</span> <a href="">Sofin Dev</a>
                        </p>
                    </div>
                    <div class="col-md-6 text-md-end">
                        <a class="vi">Điều Khoản Dịch Vụ | Chính Sách Bảo Mật</a>
                        <a class="en">Terms of Service | Privacy Policy</a>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Throttle function for performance optimization
        function throttle(func, delay = 16) {
            let timeoutId;
            let lastExecTime = 0;
            return function(...args) {
                const currentTime = Date.now();
                if (currentTime - lastExecTime > delay) {
                    func.apply(this, args);
                    lastExecTime = currentTime;
                } else {
                    clearTimeout(timeoutId);
                    timeoutId = setTimeout(() => {
                        func.apply(this, args);
                        lastExecTime = Date.now();
                    }, delay - (currentTime - lastExecTime));
                }
            };
        }

        // Smooth scrolling function
        function scrollToSection(sectionId) {
            const section = document.getElementById(sectionId);
            if (section) {
                const headerHeight = document.querySelector('.navbar').offsetHeight;
                const targetPosition = section.offsetTop - headerHeight;

                window.scrollTo({
                    top: targetPosition,
                    behavior: 'smooth'
                });
            }
        }

        // Initialize all functions when DOM is loaded
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize loader functions
            createLoaderLogoDots();
            animateLoaderOrbits();

            // Hide Loader after page load
            setTimeout(function() {
                const loaderWrapper = document.querySelector('.loader-wrapper');
                if (loaderWrapper) {
                    loaderWrapper.style.opacity = '0';
                    loaderWrapper.style.visibility = 'hidden';
                }
            }, 1000);

            // Create Loader Logo Dots
            function createLoaderLogoDots() {
                const loaderDots = document.getElementById('loader-dots');
                if (loaderDots) {
                    const numberOfDots = 20;
                    for (let i = 0; i < numberOfDots; i++) {
                        const dot = document.createElement('div');
                        dot.className = 'loader-logo-dot';
                        // Calculate position on the circle
                        const angle = (i / numberOfDots) * Math.PI * 2;
                        const radius = 80; // Radius for loader dots
                        const x = Math.cos(angle) * radius;
                        const y = Math.sin(angle) * radius;
                        // Center the dots in the 250px container
                        dot.style.left = `${x + 125}px`; // 125 = 250/2
                        dot.style.top = `${y + 125}px`;
                        // Enhanced random animation with delay
                        const sparkleDelay = Math.random() * 2;
                        const sparkleDuration = 1.5 + Math.random() * 2;
                        dot.style.animation = `sparkle ${sparkleDuration}s infinite alternate ${sparkleDelay}s`;
                        loaderDots.appendChild(dot);
                    }
                }
            }

            // Animate Loader Orbiting Dots
            function animateLoaderOrbits() {
                function animateOrbit(dotId, radius, duration, delay, direction) {
                    const dot = document.getElementById(dotId);
                    if (!dot) return;
                    let startTime = null;
                    function orbitAnimation(timestamp) {
                        if (!startTime) startTime = timestamp;
                        const elapsed = timestamp - startTime;
                        const progress = ((elapsed + delay) % duration) / duration;
                        const angle = direction ? progress * Math.PI * 2 : -progress * Math.PI * 2;
                        const x = Math.cos(angle) * radius;
                        const y = Math.sin(angle) * radius;
                        // Center the orbiting dots in the container
                        dot.style.left = `${x + 125}px`;
                        dot.style.top = `${y + 125}px`;
                        requestAnimationFrame(orbitAnimation);
                    }
                    requestAnimationFrame(orbitAnimation);
                }
                // Start orbiting animations with different parameters
                animateOrbit('loader-orbit-1', 90, 5000, 0, true);
                animateOrbit('loader-orbit-2', 100, 7000, 2000, false);
                animateOrbit('loader-orbit-3', 110, 9000, 4000, true);
            }

            // Initialize dots functions
            createLogoDots();
            animateOrbitingDots();

            // Initialize custom cursor
            const cursor = document.querySelector('.cursor');
            const cursorFollower = document.querySelector('.cursor-follower');

            if (cursor && cursorFollower && window.innerWidth > 768) {
                const updateCursor = throttle((e) => {
                    cursor.style.left = e.clientX + 'px';
                    cursor.style.top = e.clientY + 'px';
                    cursorFollower.style.left = e.clientX + 'px';
                    cursorFollower.style.top = e.clientY + 'px';
                }, 16);

                document.addEventListener('mousemove', updateCursor);

                document.addEventListener('mousedown', function() {
                    cursor.classList.add('active');
                    cursorFollower.classList.add('active');
                });

                document.addEventListener('mouseup', function() {
                    cursor.classList.remove('active');
                    cursorFollower.classList.remove('active');
                });

                // Add active class to cursor when hovering over interactive elements
                const clickables = document.querySelectorAll('a, button, .contact-info-card, .form-control, input, textarea');

                clickables.forEach(item => {
                    item.addEventListener('mouseenter', function() {
                        cursor.classList.add('active');
                        cursorFollower.classList.add('active');
                    });
                    item.addEventListener('mouseleave', function() {
                        cursor.classList.remove('active');
                        cursorFollower.classList.remove('active');
                    });
                });
            }

            // Theme Switch
            const themeSwitch = document.getElementById('theme-switch');
            const themeSwitchIcon = themeSwitch.querySelector('i');

            // Check for saved theme
            if (localStorage.getItem('theme') === 'light') {
                document.documentElement.classList.add('light-mode');
                themeSwitchIcon.classList.remove('fa-sun');
                themeSwitchIcon.classList.add('fa-moon');
            }

            themeSwitch.addEventListener('click', function() {
                document.documentElement.classList.toggle('light-mode');
                if (document.documentElement.classList.contains('light-mode')) {
                    themeSwitchIcon.classList.remove('fa-sun');
                    themeSwitchIcon.classList.add('fa-moon');
                    localStorage.setItem('theme', 'light');
                } else {
                    themeSwitchIcon.classList.remove('fa-moon');
                    themeSwitchIcon.classList.add('fa-sun');
                    localStorage.setItem('theme', 'dark');
                }
            });

            // Language Switch
            const languageSwitch = document.getElementById('language-switch');
            const currentLang = languageSwitch.querySelector('.current-lang');

            // Set default language
            // Default to English initially
            document.documentElement.lang = 'en';
            currentLang.textContent = 'EN';

            // Update placeholders function
            function updatePlaceholders() {
                const isEnglish = document.documentElement.lang === 'en';
                const inputElements = document.querySelectorAll('input[data-en-placeholder], textarea[data-en-placeholder]');

                inputElements.forEach(input => {
                    const viPlaceholder = input.getAttribute('data-vi-placeholder');
                    const enPlaceholder = input.getAttribute('data-en-placeholder');

                    if (isEnglish && enPlaceholder) {
                        input.setAttribute('placeholder', enPlaceholder);
                    } else if (!isEnglish && viPlaceholder) {
                        input.setAttribute('placeholder', viPlaceholder);
                    }
                });
            }

            languageSwitch.addEventListener('click', function() {
                const isVietnamese = document.documentElement.lang === 'vi';
                document.documentElement.lang = isVietnamese ? 'en' : 'vi';
                currentLang.textContent = isVietnamese ? 'EN' : 'VI';
                updatePlaceholders();
            });

            // Initialize placeholders
            updatePlaceholders();

            // Optimized Navbar Scroll Effect
            const navbar = document.querySelector('.navbar');
            const handleScroll = throttle(() => {
                if (window.scrollY > 50) {
                    navbar.classList.add('scrolled');
                } else {
                    navbar.classList.remove('scrolled');
                }
            }, 16);
            window.addEventListener('scroll', handleScroll, { passive: true });

            // Optimized Progress Bar Update
            const progressBar = document.querySelector('.progress-bar');
            const updateProgressBar = throttle(() => {
                const scrollTop = window.scrollY;
                const docHeight = document.body.scrollHeight - window.innerHeight;
                const scrollPercent = (scrollTop / docHeight) * 100;
                progressBar.style.width = Math.max(0, Math.min(100, scrollPercent)) + '%';
            }, 16);
            window.addEventListener('scroll', updateProgressBar, { passive: true });

            // Optimized Scroll to Top Button
            const scrollToTopBtn = document.getElementById('scroll-to-top');
            const toggleScrollToTop = throttle(() => {
                if (window.scrollY > 300) {
                    scrollToTopBtn.classList.add('show');
                } else {
                    scrollToTopBtn.classList.remove('show');
                }
            }, 100);
            window.addEventListener('scroll', toggleScrollToTop, { passive: true });

            scrollToTopBtn.addEventListener('click', function() {
                window.scrollTo({
                    top: 0,
                    behavior: 'smooth'
                });
            });

            // Optimized Reveal Animation
            const reveals = document.querySelectorAll('.reveal');
            const revealObserver = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('active');
                        revealObserver.unobserve(entry.target);
                    }
                });
            }, {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            });

            reveals.forEach(reveal => {
                revealObserver.observe(reveal);
            });

            // Optimized Smooth Scroll for all anchor links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        const headerHeight = navbar.offsetHeight;
                        const targetPosition = target.offsetTop - headerHeight;
                        window.scrollTo({
                            top: targetPosition,
                            behavior: 'smooth'
                        });
                    }
                });
            });

            // Optimized 3D Hover Effect for Cards (only on desktop)
            if (window.innerWidth > 768) {
                const cards = document.querySelectorAll('.contact-info-card, .contact-form-section, .map-container, .office-card');

                cards.forEach(card => {
                    let isHovering = false;
                    
                    const handleMouseMove = throttle((e) => {
                        if (!isHovering) return;
                        const rect = card.getBoundingClientRect();
                        const x = e.clientX - rect.left;
                        const y = e.clientY - rect.top;
                        const centerX = rect.width / 2;
                        const centerY = rect.height / 2;
                        const rotateX = (y - centerY) / 30;
                        const rotateY = (centerX - x) / 30;
                        // Use translate3d for hardware acceleration
                        card.style.transform = `translate3d(0, -5px, 0) perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg)`;
                    }, 16);

                    card.addEventListener('mouseenter', function() {
                        isHovering = true;
                        this.style.transition = 'none';
                    });

                    card.addEventListener('mousemove', handleMouseMove);

                    card.addEventListener('mouseleave', function() {
                        isHovering = false;
                        this.style.transition = 'transform 0.3s ease';
                        this.style.transform = 'translate3d(0, 0, 0) perspective(1000px) rotateX(0deg) rotateY(0deg)';
                    });
                });
            }

            // Contact Form Handler
            const contactForm = document.getElementById('contactForm');
            const messageDiv = document.getElementById('message');

            contactForm.addEventListener('submit', async function(e) {
                e.preventDefault();

                // Show loading state
                contactForm.classList.add('form-loading');
                messageDiv.classList.remove('show', 'success', 'error');

                // Get form data
                const formData = new FormData(contactForm);
                const formDataObj = Object.fromEntries(formData);

                // Simulate API call
                try {
                    await new Promise(resolve => setTimeout(resolve, 2000));

                    // Simulate successful submission
                    messageDiv.className = 'message success show';
                    if (document.documentElement.lang === 'vi') {
                        messageDiv.textContent = 'Cảm ơn bạn! Tin nhắn của bạn đã được gửi thành công. Chúng tôi sẽ liên hệ lại với bạn trong thời gian sớm nhất.';
                    } else {
                        messageDiv.textContent = 'Thank you! Your message has been sent successfully. We will contact you soon.';
                    }

                    // Reset form
                    contactForm.reset();

                    // Hide loading state
                    contactForm.classList.remove('form-loading');

                    // Scroll to message
                    messageDiv.scrollIntoView({ behavior: 'smooth', block: 'center' });

                    // Hide message after 5 seconds
                    setTimeout(() => {
                        messageDiv.classList.remove('show');
                    }, 5000);

                } catch (error) {
                    // Show error message
                    messageDiv.className = 'message error show';
                    if (document.documentElement.lang === 'vi') {
                        messageDiv.textContent = 'Có lỗi xảy ra khi gửi tin nhắn. Vui lòng thử lại sau.';
                    } else {
                        messageDiv.textContent = 'An error occurred while sending your message. Please try again later.';
                    }

                    // Hide loading state
                    contactForm.classList.remove('form-loading');

                    // Hide error message after 5 seconds
                    setTimeout(() => {
                        messageDiv.classList.remove('show');
                    }, 5000);
                }
            });

            // Optimized Newsletter Form Handler
            const newsletterForm = document.querySelector('.footer-newsletter-form');
            const newsletterInput = newsletterForm.querySelector('input');
            const newsletterButton = newsletterForm.querySelector('button');

            function validateEmail(email) {
                return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
            }

            newsletterButton.addEventListener('click', function(e) {
                e.preventDefault();
                const email = newsletterInput.value.trim();

                if (email && validateEmail(email)) {
                    // Simulate successful subscription
                    const originalText = this.innerHTML;
                    this.style.backgroundColor = '#10b981';
                    this.innerHTML = '<i class="fas fa-check"></i>';
                    this.disabled = true;
                    setTimeout(() => {
                        this.style.backgroundColor = '';
                        this.innerHTML = originalText;
                        this.disabled = false;
                        newsletterInput.value = '';
                    }, 2000);
                } else {
                    // Show error state
                    newsletterInput.style.borderColor = '#ef4444';
                    newsletterInput.style.animation = 'shake 0.5s ease-in-out';
                    setTimeout(() => {
                        newsletterInput.style.borderColor = '';
                        newsletterInput.style.animation = '';
                    }, 500);
                }
            });

            // Pause expensive animations when tab is not active
            document.addEventListener('visibilitychange', function() {
                const animations = document.querySelectorAll('[style*="animation"]');
                if (document.hidden) {
                    animations.forEach(el => {
                        el.style.animationPlayState = 'paused';
                    });
                } else {
                    animations.forEach(el => {
                        el.style.animationPlayState = 'running';
                    });
                }
            });

            // Logo animation visibility check
            const logoContainers = document.querySelectorAll('.logo-container');
            const logoVisibilityObserver = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('in-view');
                    } else {
                        entry.target.classList.remove('in-view');
                    }
                });
            }, {
                threshold: 0.1
            });

            logoContainers.forEach(container => {
                logoVisibilityObserver.observe(container);
            });

            console.log('SOFIN NETWORK Contact Page - Optimized Website initialized successfully!');
        });

        // Logo Dots Function - Optimized
        function createLogoDots() {
            const logoDots = document.getElementById('logo-dots');
            if (logoDots) {
                const numberOfDots = 12;
                const fragment = document.createDocumentFragment();

                for (let i = 0; i < numberOfDots; i++) {
                    const dot = document.createElement('div');
                    dot.className = 'logo-dot';
                    // Calculate position on circle
                    const angle = (i / numberOfDots) * Math.PI * 2;
                    const radius = 22;
                    const x = Math.cos(angle) * radius;
                    const y = Math.sin(angle) * radius;
                    dot.style.left = `${x + 25}px`;
                    dot.style.top = `${y + 25}px`;
                    // Enhanced sparkle animation with random delays
                    dot.style.animation = `sparkle ${2 + Math.random() * 2}s infinite alternate ${Math.random() * 2}s`;
                    fragment.appendChild(dot);
                }

                logoDots.appendChild(fragment);

                // Create dots for footer logo
                const footerDots = document.querySelector('.footer-logo-dots');
                if (footerDots) {
                    const footerFragment = document.createDocumentFragment();
                    for (let i = 0; i < numberOfDots; i++) {
                        const dot = document.createElement('div');
                        dot.className = 'logo-dot';
                        const angle = (i / numberOfDots) * Math.PI * 2;
                        const radius = 22;
                        const x = Math.cos(angle) * radius;
                        const y = Math.sin(angle) * radius;
                        dot.style.left = `${x + 25}px`;
                        dot.style.top = `${y + 25}px`;
                        dot.style.animation = `sparkle ${2 + Math.random() * 2}s infinite alternate ${Math.random() * 2}s`;
                        footerFragment.appendChild(dot);
                    }
                    footerDots.appendChild(footerFragment);
                }
            }
        }

        // Enhanced Orbiting Dots Function - Optimized with RAF
        function animateOrbitingDots() {
            function animateOrbit(dotId, radius, duration, delay, direction) {
                const dot = document.getElementById(dotId);
                if (!dot) return;
                
                let startTime;
                function orbitAnimation(timestamp) {
                    if (!startTime) startTime = timestamp;
                    const elapsed = timestamp - startTime;
                    const progress = ((elapsed + delay) % duration) / duration;
                    const angle = direction ? progress * Math.PI * 2 : -progress * Math.PI * 2;
                    const x = Math.cos(angle) * radius;
                    const y = Math.sin(angle) * radius;
                    dot.style.transform = `translate(${x}px, ${y}px)`;
                    requestAnimationFrame(orbitAnimation);
                }
                requestAnimationFrame(orbitAnimation);
            }

            // Start orbiting animations
            animateOrbit('orbit-dot-1', 32, 8000, 0, true);
            animateOrbit('orbit-dot-2', 27, 10000, 3000, false);

            // Footer orbit dots animation
            const footerDot1 = document.querySelector('.footer-orbit-dot-1');
            const footerDot2 = document.querySelector('.footer-orbit-dot-2');

            if (footerDot1) {
                let startTime1;
                function animateFooterDot1(timestamp) {
                    if (!startTime1) startTime1 = timestamp;
                    const elapsed = timestamp - startTime1;
                    const progress = ((elapsed) % 8000) / 8000;
                    const angle = progress * Math.PI * 2;
                    const x = Math.cos(angle) * 32;
                    const y = Math.sin(angle) * 32;
                    footerDot1.style.transform = `translate(${x}px, ${y}px)`;
                    requestAnimationFrame(animateFooterDot1);
                }
                requestAnimationFrame(animateFooterDot1);
            }

            if (footerDot2) {
                let startTime2;
                function animateFooterDot2(timestamp) {
                    if (!startTime2) startTime2 = timestamp;
                    const elapsed = timestamp - startTime2;
                    const progress = ((elapsed + 4000) % 10000) / 10000;
                    const angle = -progress * Math.PI * 2;
                    const x = Math.cos(angle) * 27;
                    const y = Math.sin(angle) * 27;
                    footerDot2.style.transform = `translate(${x}px, ${y}px)`;
                    requestAnimationFrame(animateFooterDot2);
                }
                requestAnimationFrame(animateFooterDot2);
            }
        }

        // Add performance monitoring (optional)
        if ('performance' in window) {
            window.addEventListener('load', () => {
                const perfData = performance.getEntriesByType('navigation')[0];
                console.log(`Page load time: ${perfData.loadEventEnd - perfData.fetchStart}ms`);
            });
        }

        // Add shake animation CSS
        const shakeCSS = `
            @keyframes shake {
                0%, 100% { transform: translateX(0); }
                25% { transform: translateX(-5px); }
                75% { transform: translateX(5px); }
            }
        `;
        const style = document.createElement('style');
        style.textContent = shakeCSS;
        document.head.appendChild(style);
    </script>
</body>
</html>